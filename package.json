{
  "name": "nisetsu",
  "version": "0.1.0",
  "private": true,
  "description": "NISETSU — 二節 · Hand-finished training nunchaku, made in editions of 120",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --no-port-switching"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
